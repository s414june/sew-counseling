import type { BuildSitemapIndexInput, SitemapIndexEntry, SitemapRoot } from '../../types';
export declare function buildSitemapIndex(options: BuildSitemapIndexInput): Promise<{
    sitemaps: SitemapRoot[];
    xml: string;
}>;
export declare function generateSitemapIndexXml(entries: SitemapIndexEntry[], options: {
    xsl: string | false;
    credits: boolean;
    version: string;
}): string;
